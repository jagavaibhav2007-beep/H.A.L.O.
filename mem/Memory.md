# Memory
_Current project state, active goals, and key context._

- H.A.L.O.: local resident desktop AI companion — Tauri+React UI, Python/LangGraph Brain, Python/Pipecat Voice, per Halo-PRD.md.
- Public repo: github.com/jagavaibhav2007-beep/H.A.L.O. — no secrets, standalone commit messages, small reviewable commits.
- **Phase 0 (Skeleton & Contract) is COMPLETE** — all 8 steps built, independently verified, committed and pushed (commits `6e1e3c8`, `4c9048e`). Three processes spawn, authenticate over WS, and recover from Brain crashes via backoff+respawn.
- Toolchain fully working on this machine: Rust/cargo 1.97.0 + MSVC Build Tools 2022 (C++ workload) — native Tauri builds and `cargo tauri dev` confirmed working end to end.
- `mem/` memory docs initialized this session (2026-07-10).
- Phase 0 was reliability-hardened on 2026-07-10: malformed active IPC fields are rejected, unauthenticated sockets time out, `hello_ack` gates application sends, a crash-safe lock prevents competing Brain instances, queued UI sends survive interrupted flushes, Tauri's spawn/shutdown race is closed, and `dev.ps1` no longer launches duplicate workers.
- Native Tauri lifecycle was re-verified on 2026-07-10: killing Brain caused Brain and Voice to restart on a new port and re-authenticate; graceful window close removed the Tauri, Brain, and Voice processes.
- Working pattern established: assess step complexity → dispatch Sonnet (standard) or Fable/Opus (complex) subagent → always independently re-verify subagent claims → call `advisor` before dispatching anything with non-obvious failure modes.
- **Phase 1 (Front-end shell) is IN PROGRESS.** Full plan: `phase-1-plan.md` (15 steps). Roadmap pointer: `phases.md`.
  - **Done, committed, and live-tested (2026-07-11 → 2026-07-12):**
    - **Step 1** — contract additions: `undo` (inbound), `belief_state`/`skill_state` (outbound, snapshot+delta), optional fields on `task_state`/`approval_request`/`activity`. 24 message types, all three mirrors in sync.
    - **Step 2** — mock Brain (`python -m brain --mock`): scripted `demo …` scenarios, snapshot-on-connect, reactive approval await-points. Ships with **role-based routing** (`hello.role:"ui"|"voice"`) so Voice only ever gets its contract subset — see Bugs.md.
    - **Step 3** — design tokens (`ui/src/styles/tokens.css`), glass recipe + reduced-transparency/motion fallbacks, primitives (`GlassPanel`/`Button`/`Chip`/`Icon`).
    - **Step 4** — UI event store: pure `applyFrame` reducer + zustand wrapper, replay-tested via `reducer.selfcheck.ts`.
    - **Step 5** — orb + workspace window architecture: two Tauri windows, global hotkey (`Alt+Space`, falls back to `Ctrl+Alt+Space`), manual pointer-drag with free placement (no edge-snap — reversed after user testing, see Decisions.md), **orb is user-resizable** (circle always locks to `min(width,height)` via `ResizeObserver`), tray + context menu, quit preserves the Phase-0 shutdown-flag-before-kill ordering. **User-confirmed working live** on 2026-07-12 (hotkey, workspace expand, resize, drag, all fine).
    - **Step 6** — workspace shell: sidebar (Chat/Tasks/Activity/Memory/Skills + separated Settings, amber approval-count badge, active = color + left indicator), status strip (lane chip, mic state, running-task chip with Stop), mounted-hidden view routing (scroll/state survives switching), `activeView`/`focusTarget` nav state added to the store, `Ctrl+K` + deep-jump plumbing.
    - **Step 7** — orb state language: pure `deriveOrbState` priority selector (`approval > error > task > voice`, muted always overlaid) in `reducer.ts`, unit-cased in the selfcheck; nine CSS visual states; the orb now owns its own WS connection (each webview boots its own store instance — D4); peek bubble ships as a **dedicated content-sized always-on-top window** (not inline — the orb's own window is sized to the circle and would clip it).
    - Orb visual refresh landed in the same session: `ui/src/styles/tokens.css` now defines orb-specific deep-blue/cyan/green/warm rim/glow tokens, and `ui/src/orb/OrbRoot.css` uses layered radial/linear gradients plus slow energy/rim drift animations to match the requested glossy animated sphere.
    - Steps 6–7 committed as `90c37d8` (UI/Tauri feature work) + `8a934da` (mock Brain `task_op` handler). **User-confirmed working live** on 2026-07-12 after two rounds of fixes — see Bugs.md ("orb resize/drag conflict" and "mock had no task_op handler").
    - The peek-source bug from this session is now recorded in `Bugs.md`: the old incremental skill-snapshot diff path was removed so seeded skills no longer show up as fake "new" orb peeks.
    - **Step 8** — chat view (`ui/src/chat/`: `ChatView.tsx`, `Markdown.tsx`, `ChatView.css`), wired into `WorkspaceRoot` (replaces the chat placeholder; keeps the `halo-chat-input` id for Ctrl+K). Store gained a `UserTurn|AssistantTurn` union + `appendUserTurn`/`acknowledgeInputRestore` (see Decisions.md — user messages are never echoed, so the view records them). `react-markdown@10` added (D8; raw HTML off), code blocks get copy + collapse>20 lines. Empty state (3 `demo` chips), Enter/Shift+Enter + IME guard, autoscroll-only-when-pinned, error/interrupted in-bubble states, "what I did" activity rows, voice ghost text, queued badge. **Verified:** `tsc` clean, reducer selfcheck green (added scenario 7 for the user/assistant interleave), and the render path (empty state → user bubble → input → queued badge) confirmed live in the D9 browser fallback. **Not yet visually verified against the live Brain** (streaming, markdown render, ghost text, error-restore, queued-badge-clears-on-reconnect) — needs `./dev.ps1` on a machine where the native windows are visible.
    - **Step 8 verified live** on 2026-07-12 (user confirmed streamed mock text + red approval pop-up in the floating window).
    - **Step 9** — activity feed (`ui/src/activity/`: `ActivityFeed.tsx`, `ActivityFeed.css`), wired into `WorkspaceRoot` (activity view). `@tanstack/react-virtual@3` added (D8, mandated) — newest-first virtualized timeline, variable-height rows via `measureElement`. Rows: activity icon + sentence + tier chip + lane chip + timestamp. Filters (tier/lane/task dropdowns + undoable-only toggle + text search), all client-side over the capped slice, composable. **Undo** wiring: `sendUndo(undo_token)` added to `useHaloConnection` (queue type extended to `UserMsg|TaskOpMsg|UndoMsg`); rule-3 pending — button disables on click ("Undoing…"), resolves to "Undone" only when the reversal `activity` (undoable:false, same task_id, arriving after the click via `sinceLen` scope) lands; no double-send. `undoable:false` rows show "not reversible". "New activity" pill when new entries arrive while scrolled off the top (rule 6). Empty states for no-data and no-filter-match. Mock already handled `undo` (`handle_undo`) and `demo flood`, so no Brain-side change was needed.
    - **Step 9 verified live** on 2026-07-12 via `./dev.ps1 -Mock` (the default `./dev.ps1` spawns the non-mock Phase-0 echo stub — `-Mock` is required for `demo *` triggers to do anything): `demo skill` → Undo round-trip resolved ("Undoing…" → "Undone"); `demo flood` → 2,000-row scroll stayed smooth. User also flagged a false-alarm screenshot mid-session (stale/cached dev-server bundle showing wrong chip set + orb window visually overlapping the workspace) — resolved by a full `./dev.ps1` restart; not a code bug, no fix needed.
    - **Step 10** — approval cards & trust surfaces (`ui/src/approvals/ApprovalCard.tsx` + `.css`, exporting `ApprovalOverlay` + the card). **Pure front-end** — Steps 1–2 already gave the contract (`approval_response`/`interrupt` existed) and the mock (emits `approval_request`, awaits response, branches approve/deny/edit/cancelled, cancels-on-interrupt), and Step 4's store already removed the card on the confirming frame (`resolveApprovalsForTask`). Added: `sendApprovalResponse(reply_to, decision, edited_args?)` + `sendInterrupt(conversation_id)` on `useHaloConnection` (outbound union widened to a named `Outbound` type). `ApprovalOverlay` renders a bottom-center stack over the active view (`.workspace-views`). Card: summary + tool + tier chip, expandable args, Approve(primary)/Deny(ghost)/Edit(inline args textarea → `edited_args`). Destructive variant: red border, 700ms **hold-to-approve** (rAF-driven fill so reduced-motion can't zero it; keyboard = hold Enter/Space; cancels on pointer-leave/key-up), voice-approval-off note. Rule 3: press → pending spinner, resolves only when the store drops the card. "Stop this task" link sends `interrupt` (stale-card path, distinct from Deny). Away-toast: workspace-window-only `sendNotification` when an approval arrives while hidden; click → `show_workspace` Rust command (new; wraps `open_workspace`). **Verified in D9 browser fallback** (injected frames): both variants render correctly, Approve→pending lock, Edit→textarea, confirming `activity` unmounts the card, notification import doesn't break browser mode, `tsc` clean. **Not yet verified live:** full `./dev.ps1 -Mock` round-trip, hold-to-approve firing at 700ms, and the Windows toast + click→show_workspace (Tauri-only; needs cargo rebuild of the changed `windows.rs`/`lib.rs`).
    - **Step 11** — tasks view & lane chip (`ui/src/tasks/TasksView.tsx` + `.css`), wired into `WorkspaceRoot` (tasks view, replaces placeholder). Cards ordered running → waiting_approval → paused → recent-done (done collapses after 24h by `ts`). Each card: title, state chip (icon+text), progress bar (`step/steps_total` + `step_label`), lane **pin dropdown** (`<select>` → `sendLanePin`), pause/resume/stop → `task_op`. Rule 3 throughout — a task's controls lock on press and clear only when a fresh `task_state` (new object reference) confirms. Paused cards show `reason` + "will continue from step N" (resumability visible). Sandbox (lane 3) cards host a **stream tile** rendering the latest `stream_frame` jpeg. Store gained a `streams` slice (`stream_frame` case drops stale frames by `seq`; reducer.selfcheck scenario 8 covers it) + `selectStream`. `sendLanePin` added to `useHaloConnection` (outbound `Outbound` union). Mock gained `handle_lane_pin` (confirms with a task_state) + `demo stream` scenario (lane-3 task cycling 3 tiny scripted JPEGs, seq 0-5) + `lane_pin` dispatch in `server.py`; `test_mock.py` check 6 now sweeps `demo stream`. **Verified:** `tsc` clean, reducer selfcheck green (incl. seq-drop), `test_mock.py` + contract sync green, and in the D9 browser fallback (injected frames) all five card states render in the correct order, the sandbox JPEG decodes (naturalWidth=64), Stop locks to "Stopping…" (both buttons disabled), and a lane-pin change disables the select — rule 3 confirmed. **Not yet verified live:** full `./dev.ps1 -Mock` `demo task`/`demo stream` walk (needs the native app).
    - **Step 12** — memory panel (`ui/src/memory/MemoryView.tsx` + `.css`), wired into `WorkspaceRoot` (memory view). Active beliefs grouped by kind (preference/project/workflow/decision/lesson), salience-sorted, searchable. Provenance chip is the star — 🗣 "you said" (User icon, success tone + left accent) vs ✨ "Halo inferred" (Sparkles, primary). Inline **edit** → `memory_edit{op:"edit"}`; **soft-delete** → 5s "Undo" toast, then `memory_edit{op:"delete"}` at expiry (card stays until the confirming `belief_state:archived` lands — never a local mutation, rule 3); **restore** from the archived filter and from superseded-chain history → `memory_edit{op:"restore"}`. Superseded chains expand (predecessors found by `superseded_by`), each version restorable. Archived filter toggle. `sendMemoryEdit` added to `useHaloConnection` (Outbound union). Mock gained a live `_beliefs` registry (seeded once, mutated by edits so round-trips are real and reconnect snapshots re-push current state, D6), `handle_memory_edit` (edit→supersede old + emit user-stated new; delete→archived; restore→active), `memory_edit` dispatch in `server.py`; `_scenario_memory` now updates the registry too. **Verified:** `tsc` clean, `test_mock.py` + contract sync green, and in the D9 browser fallback (injected frames) — 5 kind groups render, provenance chips correct, edit round-trip flips provenance to "you said" + moves the original into the history chain, soft-delete toast + undo works, archived filter shows the archived belief with Restore. **Not yet verified live:** full `./dev.ps1 -Mock` `demo memory` walk (auto-correct peek + deep-jump).
    - **Step 13** — skills panel (`ui/src/skills/SkillsView.tsx` + `.css`) and settings view (`ui/src/settings/SettingsView.tsx` + `.css`), both wired into `WorkspaceRoot`. Skills: Auto-learned ✨ / User-made 🛠 groups + a Playbooks filter, success-rate bar reddening under 60% (visible before the auto-retire threshold fires), trial-run drawer fed by narrated `activity` on a synthetic `skill-trial-{name}` task_id, Retired section (grayed, reason + Restore — nothing hard-deletes, same ethos as memory). Settings: General (hotkey via the `active_hotkey` Tauri command, theme select, disabled launch-at-startup with an honest note), Voice (two toggles that record intent via `settings_update` — Voice itself stays the idle stub until Step 14), Models (static honest placeholder IDs + live month spend from `spend_update`), Keys & connections (●●● status dots, disabled), collapsed Advanced (read-only defaults). `sendSkillOp`/`sendSettingsUpdate` added to `useHaloConnection`. Mock gained a live `_skills` registry (same pattern as `_beliefs`), `handle_skill_op` (trial→narrated dry-run activity, no state change; disable→paused; restore→active, reason cleared; delete→retired, soft — schema has no "deleted" status), `skill_op` dispatch in `server.py`; `_scenario_skill`'s birth now updates the registry too. `theme.ts` gained `watchThemeAcrossWindows()` (a `storage`-event listener, since that event only fires in the *other* window — needed for the "theme switch is instant across both windows" acceptance criterion), wired once in `main.tsx`.
    - **Bug found and fixed while verifying Step 13 live:** the rule-3 "unlock on confirm" effect in Tasks/Memory/Skills views mutated a ref inside a `setState` functional updater, which is silently broken under React StrictMode's double-invoke-updaters-in-dev behavior — buttons locked correctly but never unlocked. Fixed in all three views (see mem/Bugs.md). This had never actually been exercised end-to-end in Steps 11–12's verification (only the *lock* step was tested), a gap closed this step.
    - **Verified:** `tsc` clean, `test_mock.py` (incl. new skill_op round-trip check) + contract sync green, and in the D9 browser fallback (injected frames) — all skill groups/filter/trial-drawer/rate-bar render correctly, the lock→confirm→unlock cycle now works for Skills (and was spot-checked working for Tasks after the fix), Settings renders all groups with live spend and honest disabled-state notes, theme toggle applies instantly and persists. **Not yet verified live:** full `./dev.ps1 -Mock` `demo skill` walk, and the actual cross-window instant theme sync (mechanism is standard `storage`-event listening, not exercisable with a single browser tab).
    - **Step 14** — voice presence (scripted). Most of it already existed (orb voice states from Step 7, chat ghost text from Step 8, `_scenario_voice`). Added: (1) **mute round-trip** — chat mic is now a `<button>` calling `sendMic` (new hook sender), mock `handle_mic` replies `voice_state:muted`/`idle`, `mic` dispatch in `server.py`; muted shows red slashed mic in chat + "Muted" in status strip + `deriveOrbState`→"muted" **simultaneously** (verified). (2) **barge-in beat** in `_scenario_voice` — mid-reply `voice_state:listening` then back to speaking (pulse stops instantly). (3) **degraded modes** `demo tts-down` / `demo stt-down` — scripted scenarios emitting a narrated `activity` with cause + way forward (no new voice_state needed — the contract enum has none, so an honest note is the lazy-correct surface). (4) **transcript final→real bubble** — reducer `transcript` case: `final:true` solidifies the spoken text into a `UserTurn` (new `viaVoice?` flag → mic glyph) and clears the ghost (no duplicate bubble); partials stay ghost. reducer.selfcheck scenario 9 + test_mock check 10 cover the new logic.
    - **Test-isolation flake fixed while here:** `check_same_conversation_is_serialized` sent a generic "second turn" whose tokens kept streaming after the check closed its socket, leaking into the *next* check's freshly-connected client (broadcast targets all authenticated clients, D4) — was intermittently green, now consistently failed once check ordering shifted. Fixed by draining that turn to its `done` before the check returns.
    - **Verified:** `tsc` clean, reducer selfcheck (incl. new voice scenario) green, `test_mock.py` (10 checks, 3× stable) + contract sync green, and D9 browser fallback — partial→ghost, final→real bubble+glyph (no dup), mute unambiguous across chat/status/orb, unmute clears. **Not yet verified live:** full `./dev.ps1 -Mock` `demo voice` walk (the actual orb animations + barge-in visuals), orb-menu mute (still a Rust no-op — needs the orb window's WS, deferred; the chat mic button covers the round-trip).
  - **Not started:** Step 15 (demo walkthrough, a11y/perf pass & phase E2E). **Step 15 (final) is next — closes Phase 1.**
- Two bugs were found only by *running the real stack*, not by green tests — see Bugs.md. Standing lesson: for anything with a visual/runtime surface, an automated gate passing is not sufficient evidence; launch the actual app.
