# Memory
_Current project state, active goals, and key context._

- **2026-07-27 UX/state stabilization sweep complete.** User-reported real-app regressions from the post-Phase-2 polish pass were fixed end-to-end: correlated/global errors now surface honestly, archived memory and superseded history rehydrate on demand after restart, denied/stopped approvals always clear their modal blocker, Stop terminates the queued approval/tool path instead of continuing, redacted approval edits preserve untouched original values, and untouched New chat tabs are discarded instead of being saved into Recent. Evidence now lives in `mem/Bugs.md` entries dated 2026-07-27. Verification: the broader sweep reached a full `./dev.ps1 -Verify` green gate earlier in the session; the final empty-chat follow-up was re-checked with `node ui/src/state/conversations.selfcheck.ts`, `npx tsc --noEmit`, `npm test -- --run` (32/32), `npm run build`, and a native no-mock replay under `./dev.ps1`.

- H.A.L.O.: local resident desktop AI companion — Tauri+React UI, Python/LangGraph Brain, Python/Pipecat Voice, per Halo-PRD.md.
- Public repo: github.com/jagavaibhav2007-beep/H.A.L.O. — no secrets, standalone commit messages, small reviewable commits.
- Toolchain on this machine: Rust/cargo 1.97.0 + MSVC Build Tools 2022 (C++ workload), native Tauri builds confirmed working.
- **Phase 0 (Skeleton & Contract) — COMPLETE.** Three processes (Tauri parent, Brain, Voice) spawn, authenticate over WS, recover from crashes via backoff+respawn. Hardened 2026-07-10: malformed IPC fields rejected, unauth sockets time out, `hello_ack` gates sends, crash-safe Brain lock, queued sends survive interrupted flushes, no duplicate-worker launch.
- **Phase 1 (Front-end shell) — COMPLETE** (2026-07-13), all 15 steps (contract, mock Brain, design tokens, event store, orb+workspace windows, sidebar/status strip, orb state language, chat, activity feed, approvals, tasks, memory panel, skills+settings, voice presence, E2E gate). Full detail in git history / `phase-1-plan.md`. Native walkthrough user-confirmed 2026-07-13. Later Phase 2 status is recorded below; this historical entry no longer claims Phase 2 has not started.
- **Ponytail refactor sweep — COMPLETE** (2026-07-15, commits `b67f5aa`..`f66337e`). Net ~-640 lines: rule-3 lock/unlock unified into `usePendingConfirm.ts`, shared `.halo-*` CSS primitives adopted, legacy `App.tsx` deleted, dead `focusTarget` removed, mock Brain frame boilerplate single-sourced. See Patterns.md for what was audited and deliberately left alone.
- **Companion capsule redesign — COMPLETE** (2026-07-15/17). Orb → horizontal Midnight Blue capsule (lane/task/orb/approval/mic chips). Native HWND clipping (`SetWindowRgn`) added 2026-07-17 so the pill shape is real at the Windows level, not just CSS. See Decisions.md.
- **2026-07-17 IPC/supervision/UI hardening** — validated executable Phase-1 IPC fields in both mirrors, fixed capped-activity arrival boundaries, destructive-approval pointer filtering, mock snapshot disconnect cleanup, sidecar polling/startup ownership, oversized restored-window clamping. Launcher (`./dev.ps1`) now stable/attached by default (`-WatchNative` opts into hot reload). Committed as `efa4dd7`.
- **2026-07-17 autonomous debugging loop** (DevFleet skill invoked; no DevFleet MCP server running, substituted native `Agent` subagents — same capability). 5 parallel read-only reviewers (Rust/Brain/Mock+Voice/UI-transport/UI-views) swept the whole repo; driver verified every finding against source before fixing (hard triage bar: concrete input→wrong-output only). Rust and UI-transport/state came back clean. 7 real defects found and fixed across 3 commits (`555176f`, `d54eaeb`, `8f33a47`) — see Bugs.md entries dated 2026-07-17 (bug hunt). Deliberately deferred as speculative-on-clean-code (Phase-2-shaped, revisit when the real Brain lands): retained `asyncio.create_task` refs in server.py, inbound Brain→UI enum/type validation in `contract.ts`, a global error-frame surface for `conversation_id`-less errors.
- Working pattern: assess complexity → dispatch cheapest-capable subagent model → always independently re-verify subagent claims against real source before fixing → call `advisor` before dispatching anything with non-obvious failure modes, and before/after any autonomous multi-batch loop.
- **2026-07-18 second ponytail shortening pass (pre-Phase-2)** — Opus-driven, subagent inventory per process tree (Rust / TS / Brain / Voice / Shared). Baseline gates green. **Outcome: codebase already lean — exactly ONE code change warranted.** The 07-15 sweep + 07-17 hardening left no meaningful dead code: all Rust commands wired, mock.py already factored (`_swallow_closed`/`_activity`/`_stream_words`/`_run_reorg_steps`), contract validators are trust-boundary/parity-protected, remaining dups are ≤4-line or line-wash-on-abstraction — all correctly left alone (manufacturing churn on validation/parity code would have been the anti-pattern). The one real dedup: the byte-identical store-connection wiring in OrbRoot/WorkspaceRoot, extracted to `ui/src/state/useStoreConnection.ts` and **verified live** (both orb+workspace windows authenticate as role=ui through the shared hook under `./dev.ps1 -Mock` — the only way to prove it, since transport code has no selfcheck). See Decisions.md. Static gates (tsc, 4 UI selfchecks, cargo build+test, Python smoke/phase1/contract-sync) all green after.
- **2026-07-22 confabulation bug found during Phase 2 human verification** — user asked Halo to find an existing Desktop PDF; Halo falsely reported it didn't exist. Root cause: `gate.py` `_execute_tail` discarded every data-returning tool's return value on success, sending the model a content-free `"I ran {tool}."` regardless of what was found — a hit and a miss were indistinguishable. Fixed: tool results now serialize into the message (capped, explicit "no matches" for empty), `_resolve` anchors relative paths at home not CWD, system prompt states accessible roots. Full detail in Bugs.md's top entry. Two subagents independently traced the pipeline before an Opus agent implemented + tested the fix; all fixes re-verified by a third pass (fresh test runs + direct code read) before being accepted.
- **Phase 2 (Backend spine) — COMPLETE** (2026-07-21, Steps 1-10). The real Brain (`brain/brain/`) replaces echo as the default (non-`--mock`) handler: SQLite store (beliefs/actions/tasks/spend/settings, D3), `keyring`-backed secrets (D8), async OpenRouter client + rule-based light/heavy router (D5), a LangGraph chat spine with SqliteSaver checkpointing (thread_id = conversation_id), the permission gate + tool registry choke point (D4), real activity log + undo (D7), Lane-1 local file tools (read/create/edit/move/organize + allowlisted read-only commands) under the gate, real memory (extraction/dedupe/provenance-enforced supersede/decay/panel round-trips), and a real snapshot-on-connect with history summarization and spend rollup. Contract stayed frozen throughout (D2) — `check_contract_sync.py` still reports 25 types, zero drift. Gate: `shared/phase2_check.py` (real Brain, `HALO_LLM_STUB`/`HALO_EXTRACT_STUB` offline seams) now runs after Phase 0/1 in `./dev.ps1 -Smoke`. All of Steps 1-9's `brain/tests/test_*.py` plus `phase2_check.py` green; native Phase-2 checklist appended to VERIFY.md (requires a real OpenRouter key — not yet user-run). **Phase 3 (heavy systems) has not started.**
- **2026-07-22 audit closeout.** Scope/evidence: `AUDIT_PLAN.md`. `./dev.ps1 -Verify` is green end-to-end (all Python/Voice scripts, five TS self-checks, Vitest 16/16, production build, Rust 7/7, Phase 0/1/2 gates). App-scoped native mock launch authenticated Brain/Voice; forced exact Tauri PID death reaped exact Brain/Voice children. Major durable fixes include complete IPC schemas, path/command confinement, rollback-safe undo and memory correction, SQLite serialization, honest stream errors/cancellation, bounded sends/turn admission/lock reclamation, supervised tasks, operation-error correlation, state snapshot convergence, Settings honesty, native Job Object ownership, and accessibility/focus/contrast fixes. No validated P0/P1 remains. Deferred: `dir_organize` cooperative progress/cancel awaits a real task runtime; human WebView/NVDA/minimum-size checks and a real-key OpenRouter walkthrough remain in `VERIFY.md`. Phase 3 has not started.

- **2026-07-23 Phase 3 readiness audit — see [PHASE3_READINESS_AUDIT.md](../PHASE3_READINESS_AUDIT.md).** Three-subagent scan (design ceilings / bugs+UI connections / OSS landscape), Fable-synthesized. 3 P1 ceilings (no task runtime; snapshot-growth × deferred-cap livelock; no contract versioning + close-on-unknown parser), 2 live bugs (real-Brain `mic` silent no-op; unretained `note_turn` task), latent `skill_op` gap + non-atomic v2 migration, 6 P2s. Plan: Tranche A (correctness fixes) + Tranche B (task runtime, checkpoint retention, sidecar resilience, UI state bounds) gate Phase 3a; Tranche C is doc/prep (local voice stack swap to faster-whisper+Kokoro, SKILL.md format, PyInstaller prototype). OSS verdict: keep homegrown memory/approval/supervisor; borrow patterns, adopt no frameworks. **Same day: Tranche A (A1-A6) + Tranche C docs (C1-C2) implemented via three parallel subagents and verified — full gate green (contract sync, all Python/TS/Rust tests, Vitest 16/16, production build, Phase 0/1/2 gates); one flake: McAfee transiently LNK1104-locks freshly linked cargo test exes, passes on retry. B1 task-runtime design written (systemdesign/12-task-runtime.md + techstack/12) — awaiting review before B2 implementation. Added a `note_turn` test (`test_memory.py` check 10 — the entry point of memory v2 and the A2 fix path had zero direct coverage before). Three bugs found+fixed this session recorded in Bugs.md; two new decisions (contract versioning, task-runtime split) and two new patterns (embed-before-lock, semaphore-at-choke-point) recorded.**
- **2026-07-23 memory v2 (M1–M3) implemented** — per-turn extraction retired. Design: `systemdesign/03-memory.md` (v2); decision record in Decisions.md; schema v2 in MigrationLog.md. `note_turn` marks a conversation dirty and arms an idle timer (`HALO_MEMORY_IDLE_S`, default 1800s); `consolidate_span` runs ONE extraction over the un-consolidated span, a mem0-style ADD/UPDATE/INVALIDATE/NOOP decision per candidate against top-5 vector neighbors (v1's distance+first-3-words heuristics ARE the deterministic stub/no-key decision path), writes one `session_summary` row, and advances a crash-safe cursor only on full success. Triggers: idle / graceful shutdown (`graph.aclose`→`flush_all`) / startup recovery / 8k-token pressure. Snapshot hydration now live+active only, cap 50 (was: up to 100 rows, all statuses, 60% dead). Beliefs carry `valid_at`/`invalid_at` (Graphiti). Rule 6 stays store-enforced below the LLM decision layer. All gates green post-change (brain tests, contract sync, smoke, phase1, phase2) + live real-key run verified: no per-turn broadcasts, one consolidation after idle, restatement deduped to one row, real `_llm_summary` JSON written. M4 (memory_query panel history + Sessions UI) not started; real-key AUDN paths otherwise untested by automated gates (marked ponytail in graph.py).
- **2026-07-28 whole-repo deep-scan audit — see [DEEPSCAN_AUDIT.md](../DEEPSCAN_AUDIT.md).** Eight parallel Opus scanners (Brain runtime/transport, Brain data+graph, tools/mock/voice/contract, Rust/Tauri+build, UI state/IPC, UI/UX+a11y via `ui-ux-pro-max`, third-party API hallucination check via context7, docs-drift+ponytail-ledger) covered the whole codebase; several were killed by session limits and re-dispatched until all eight completed. Baseline was green throughout (16/16 Brain suites, 29 contract schemas synced, tsc clean, Vitest 32/32) — the finding is that green gates were hiding 8 P0s the test suite structurally cannot see (see Bugs.md 2026-07-28 entries). API check came back clean: 104 surfaces verified, zero hallucinated. Docs-drift found no committed secrets and no dead code; all 68 `ponytail:` markers are valid deferrals.
  - **Fixed and verified this session (P0-1 through P0-7, both rule-3 P1 hangs):** memory vector search silently starved by dead index rows; undo/edit CRLF corruption; `dir_organize` partial-failure data loss; Settings key permanent lock; approval-card dead-modal on a dispatch throw; `hello_ack`-flush socket deafness; approval overlay blocking the composer; link contrast + missing `color-scheme`; `langgraph`/`aiosqlite` dependency pins; `voice/tests/test_client.py` real-DB write isolation. All Python fixes gate-verified (16/16 Brain suites); all UI fixes tsc-clean + Vitest 33/33 with new regression tests for the two highest-risk bugs.
  - **Still open, ranked, full detail + file:line in [DEEPSCAN_AUDIT.md](../DEEPSCAN_AUDIT.md) §7:** (1) **P0-8** — capsule clips the mic indicator when narration renders at the fixed 360×52 size (`OrbRoot.css`); needs the running app to pin the truncation width, deferred for that reason. (2) **UI/UX P1 polish** (Tranche 0d, wants `ui-ux-pro-max` + live app) — composer stuck at one row (`Shift+Enter` unusable), an arriving approval steals composer focus and arms Deny under a keystroke, Status-strip Stop button clips at `minWidth:720`, card titles can't ellipsize, tier-3 destructive color used as text at 3.12:1, capsule leaks theme tokens, 12 WCAG a11y blockers with near-zero test coverage, and an unresolved decision on whether to implement or delete the unused Midnight-Blue `--canvas`/`--midnight` tokens. (3) **Tranche 1 — unbounded growth**: `checkpoints.db` grows quadratically and is never pruned (already 7MB+), `action` table has no retention/index, `_dirty` map never clears after consolidation, `digest_cache` never evicts stale-sha rows. (4) **Tranche 2 — correctness hardening**: no size guard on `file_read` (can OOM the Brain), non-atomic file writes, no page/sheet caps on doc parsing, `doc_digest` extracts-before-hash (cache can permanently serve stale content), `_release_deferred` broadcast-ordering bug, stalled-client socket never closed, `_EMBED_LOCK` needed around the embedder's cold-start race, reserved-`id` contract assertion missing, `SpendUpdateMsg` TypedDict drift, `_MOCK_DISPATCH` has no enumeration test, `skill_op` has no error-correlation mapping, and the legacy back-fill for already-stranded active beliefs (see Decisions.md) is still undone. (5) **Tranche 3 — packaging blockers**: `repo_root()` bakes in this machine's compile-time path, `python -m brain` needs bundled sidecar binaries, release builds will open a visible console per sidecar, all native diagnostics vanish in release, CSP is `null`, and a `child.kill()` failure branch abandons supervision instead of re-entering the backoff ladder. (6) **Tranche 4 — doc corrections only, no code**: 5 stale status claims (`systemdesign/14-token-economics.md` and `03-memory.md` both declare shipped work "not built"), Tier-1-activity vs `04-permissions.md` drift, stale `deriveOrbState`/`focusTarget` references in CLAUDE.md, deprecated `mammoth.convert_to_markdown`, 2 dead npm plugin deps, `techstack/` missing an `11`, and contract-1.1's memory-history surface has zero gate coverage.
  - **Not yet committed.** The fixes above sit in the same uncommitted working tree as a pre-existing 50-file token-cost-reduction sweep — a commit decision (bundle vs. split onto its own branch) is still open.

- **2026-07-24 document ingestion (3-layer) implemented** — root-caused a live test where reading 8 markdown files burned ~68k tokens and escalated to the HEAVY model (no extraction layer + full-history retransmission every tool round + blanket LIGHT-failure escalation). Design: `systemdesign/13-document-ingestion.md` + `techstack/13` (three background research subagents surveyed OSS designs first — markitdown/docling/pymupdf4llm/unstructured, OpenHands condenser/Anthropic context-editing/Aider repo-map, LangGraph map-reduce/LlamaIndex/LLMLingua — provenance table in the design doc). Implemented via 3 parallel subagents (model-by-complexity: Sonnet/Opus/Fable), all independently re-verified together after: **Layer 0** `brain/brain/extract.py` (new extension→markdown dispatcher: pypdfium2/pypdf/mammoth/openpyxl/markdownify) + `file_read` cap 64KB→8KB with offset/limit paging + head+tail command-output truncation. **Layer 1** `_prompt_messages` in `graph.py` now stubs already-answered tool results >500 chars with byte-stable restorable placeholders (pure projection, checkpoint untouched — this is the fix for the quadratic retransmission). **Layer 2** new `doc_digest` Lane-1 tool (`brain/brain/tools/docs.py`): parallel per-doc LIGHT-model map calls → JSON digest → one reduce call, only ~1-2k tokens ever reach chat history; digest cache keyed by (path, sha256, version) in new `digest_cache` table (schema v3, MigrationLog.md). Rejected on evidence: pymupdf4llm (AGPL), docling (kept as future optional deep-parse tier, ~1GB torch), unstructured (Windows-hostile system binaries), LLMLingua (GPU-heavy, hurts small models most). Full combined gate green (`./dev.ps1 -Smoke`: Phase 0/1/2 all pass, including new `doc_digest` phase2_check criterion) plus 8 brain unit self-checks. Manual real-key re-run of the original 8-file scenario against actual spend not yet performed by the user.

- **2026-07-29 DEEPSCAN_AUDIT.md remediation — COMPLETE, committed & pushed (`116d4af` on `token-cost-reduction`, not yet merged to `main`).** Every P0/P1, the full §7 tranche plan (0d packaging/UI-a11y through 4 doc corrections), and every concretely-identifiable P2/P3 are done, plus a re-scanned UI/UX polish tail (see below) and two product decisions the audit left open (card radius, loading spinners — see Decisions.md). Driven via 5 parallel Opus agents on disjoint file ownership + driver-direct fixes for anything cross-cutting (see Patterns.md "Large multi-agent sweep"); all 5 were killed mid-edit by a shared session-limit reset and recovered by gate-diagnosis rather than blind re-dispatch (see Patterns.md "Recovering from agents killed mid-edit"). Headline fixes: P0-8 capsule mic-clip closed with empirical browser measurement (not source-reading); memory retrieval/undo/dir_organize/Settings-lock/approval-dead-modal (already fixed 07-28) untouched; new this pass — checkpoint pruning + `action` retention under schema v4 (MigrationLog.md), `doc_digest` hash-before-extract with no degraded-digest caching, `_EMBED_LOCK`, a new `snapshot_complete` contract type, `skill_op` rule-3 correlation, atomic file writes, `CREATE_NO_WINDOW` + runtime `repo_root()` + a real CSP, and a from-memory Midnight-Blue-backdrop/`--motion-slow` claim that turned out false and was actually implemented on re-scan. Three regressions were introduced and caught by re-running gates mid-session (not assumed green): a dead-branch removal that broke a live "no matches" guard, a D2-suppression/Layer-1-stub contradiction, and a stale `phase2_check` assertion certifying the exact behavior a fix removed — all in Bugs.md dated 2026-07-29. Two audit-baseline claims were found wrong while implementing: `tsc` was not actually clean (Vitest doesn't typecheck), and the "already implemented" Midnight-Blue/motion claim wasn't. Full gate green at close: all Brain suites, `check_contract_sync` (30 schemas, now covering TypedDicts), phase1/phase2/voice, `tsc`, Vitest 40/40, 4 TS selfchecks, `cargo build`+test (9 tests). **Next:** open a PR to merge `token-cost-reduction` → `main` (5 commits behind); human-only VERIFY.md items (NVDA pass, real-key OpenRouter walkthrough) still outstanding.
