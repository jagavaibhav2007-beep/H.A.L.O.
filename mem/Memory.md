# Memory
_Current project state, active goals, and key context._

- H.A.L.O.: local resident desktop AI companion — Tauri+React UI, Python/LangGraph Brain, Python/Pipecat Voice, per Halo-PRD.md.
- Public repo: github.com/jagavaibhav2007-beep/H.A.L.O. — no secrets, standalone commit messages, small reviewable commits.
- **Phase 0 (Skeleton & Contract) is COMPLETE** — all 8 steps built, independently verified, committed and pushed (commits `6e1e3c8`, `4c9048e`). Three processes spawn, authenticate over WS, and recover from Brain crashes via backoff+respawn.
- Toolchain fully working on this machine: Rust/cargo 1.97.0 + MSVC Build Tools 2022 (C++ workload) — native Tauri builds and `cargo tauri dev` confirmed working end to end.
- `mem/` memory docs initialized this session (2026-07-10).
- Phase 0 was reliability-hardened on 2026-07-10: malformed active IPC fields are rejected, unauthenticated sockets time out, `hello_ack` gates application sends, a crash-safe lock prevents competing Brain instances, queued UI sends survive interrupted flushes, Tauri's spawn/shutdown race is closed, and `dev.ps1` no longer launches duplicate workers.
- Native Tauri lifecycle was re-verified on 2026-07-10: killing Brain caused Brain and Voice to restart on a new port and re-authenticate; graceful window close removed the Tauri, Brain, and Voice processes.
- Working pattern established: assess step complexity → dispatch Sonnet (standard) or Fable/Opus (complex) subagent → always independently re-verify subagent claims → call `advisor` before dispatching anything with non-obvious failure modes.
- **Phase 1 (Front-end shell) is IN PROGRESS.** Full plan: `phase-1-plan.md` (15 steps). Roadmap pointer: `phases.md`.
  - **Done, committed, and live-tested (2026-07-11 → 2026-07-12):**
    - **Step 1** — contract additions: `undo` (inbound), `belief_state`/`skill_state` (outbound, snapshot+delta), optional fields on `task_state`/`approval_request`/`activity`. 24 message types, all three mirrors in sync.
    - **Step 2** — mock Brain (`python -m brain --mock`): scripted `demo …` scenarios, snapshot-on-connect, reactive approval await-points. Ships with **role-based routing** (`hello.role:"ui"|"voice"`) so Voice only ever gets its contract subset — see Bugs.md.
    - **Step 3** — design tokens (`ui/src/styles/tokens.css`), glass recipe + reduced-transparency/motion fallbacks, primitives (`GlassPanel`/`Button`/`Chip`/`Icon`).
    - **Step 4** — UI event store: pure `applyFrame` reducer + zustand wrapper, replay-tested via `reducer.selfcheck.ts`.
    - **Step 5** — orb + workspace window architecture: two Tauri windows, global hotkey (`Alt+Space`, falls back to `Ctrl+Alt+Space`), manual pointer-drag with free placement (no edge-snap — reversed after user testing, see Decisions.md), **orb is user-resizable** (circle always locks to `min(width,height)` via `ResizeObserver`), tray + context menu, quit preserves the Phase-0 shutdown-flag-before-kill ordering. **User-confirmed working live** on 2026-07-12 (hotkey, workspace expand, resize, drag, all fine).
    - **Step 6** — workspace shell: sidebar (Chat/Tasks/Activity/Memory/Skills + separated Settings, amber approval-count badge, active = color + left indicator), status strip (lane chip, mic state, running-task chip with Stop), mounted-hidden view routing (scroll/state survives switching), `activeView`/`focusTarget` nav state added to the store, `Ctrl+K` + deep-jump plumbing.
    - **Step 7** — orb state language: pure `deriveOrbState` priority selector (`approval > error > task > voice`, muted always overlaid) in `reducer.ts`, unit-cased in the selfcheck; nine CSS visual states; the orb now owns its own WS connection (each webview boots its own store instance — D4); peek bubble ships as a **dedicated content-sized always-on-top window** (not inline — the orb's own window is sized to the circle and would clip it).
    - Orb visual refresh landed in the same session: `ui/src/styles/tokens.css` now defines orb-specific deep-blue/cyan/green/warm rim/glow tokens, and `ui/src/orb/OrbRoot.css` uses layered radial/linear gradients plus slow energy/rim drift animations to match the requested glossy animated sphere.
    - Steps 6–7 committed as `90c37d8` (UI/Tauri feature work) + `8a934da` (mock Brain `task_op` handler). **User-confirmed working live** on 2026-07-12 after two rounds of fixes — see Bugs.md ("orb resize/drag conflict" and "mock had no task_op handler").
    - The peek-source bug from this session is now recorded in `Bugs.md`: the old incremental skill-snapshot diff path was removed so seeded skills no longer show up as fake "new" orb peeks.
    - **Step 8** — chat view (`ui/src/chat/`: `ChatView.tsx`, `Markdown.tsx`, `ChatView.css`), wired into `WorkspaceRoot` (replaces the chat placeholder; keeps the `halo-chat-input` id for Ctrl+K). Store gained a `UserTurn|AssistantTurn` union + `appendUserTurn`/`acknowledgeInputRestore` (see Decisions.md — user messages are never echoed, so the view records them). `react-markdown@10` added (D8; raw HTML off), code blocks get copy + collapse>20 lines. Empty state (3 `demo` chips), Enter/Shift+Enter + IME guard, autoscroll-only-when-pinned, error/interrupted in-bubble states, "what I did" activity rows, voice ghost text, queued badge. **Verified:** `tsc` clean, reducer selfcheck green (added scenario 7 for the user/assistant interleave), and the render path (empty state → user bubble → input → queued badge) confirmed live in the D9 browser fallback. **Not yet visually verified against the live Brain** (streaming, markdown render, ghost text, error-restore, queued-badge-clears-on-reconnect) — needs `./dev.ps1` on a machine where the native windows are visible.
    - **Step 8 verified live** on 2026-07-12 (user confirmed streamed mock text + red approval pop-up in the floating window).
    - **Step 9** — activity feed (`ui/src/activity/`: `ActivityFeed.tsx`, `ActivityFeed.css`), wired into `WorkspaceRoot` (activity view). `@tanstack/react-virtual@3` added (D8, mandated) — newest-first virtualized timeline, variable-height rows via `measureElement`. Rows: activity icon + sentence + tier chip + lane chip + timestamp. Filters (tier/lane/task dropdowns + undoable-only toggle + text search), all client-side over the capped slice, composable. **Undo** wiring: `sendUndo(undo_token)` added to `useHaloConnection` (queue type extended to `UserMsg|TaskOpMsg|UndoMsg`); rule-3 pending — button disables on click ("Undoing…"), resolves to "Undone" only when the reversal `activity` (undoable:false, same task_id, arriving after the click via `sinceLen` scope) lands; no double-send. `undoable:false` rows show "not reversible". "New activity" pill when new entries arrive while scrolled off the top (rule 6). Empty states for no-data and no-filter-match. Mock already handled `undo` (`handle_undo`) and `demo flood`, so no Brain-side change was needed.
    - **Step 9 verified live** on 2026-07-12 via `./dev.ps1 -Mock` (the default `./dev.ps1` spawns the non-mock Phase-0 echo stub — `-Mock` is required for `demo *` triggers to do anything): `demo skill` → Undo round-trip resolved ("Undoing…" → "Undone"); `demo flood` → 2,000-row scroll stayed smooth. User also flagged a false-alarm screenshot mid-session (stale/cached dev-server bundle showing wrong chip set + orb window visually overlapping the workspace) — resolved by a full `./dev.ps1` restart; not a code bug, no fix needed.
  - **Not started:** Steps 10–15 (approvals/tasks/memory/skills+settings panels, voice presence, phase E2E). **Step 10 (approval cards & trust surfaces) is next — highest-risk/highest-value panel.**
- Two bugs were found only by *running the real stack*, not by green tests — see Bugs.md. Standing lesson: for anything with a visual/runtime surface, an automated gate passing is not sufficient evidence; launch the actual app.
